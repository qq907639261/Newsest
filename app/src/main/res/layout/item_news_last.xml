<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View"/>

        <variable
            name="news"
            type="com.xhbb.qinzl.newsest.viewmodel.News"/>

        <variable
            name="newsTotalPageOuted"
            type="boolean"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="8dp">

        <include
            layout="@layout/item_news"
            app:news="@{news}"/>

        <ProgressBar
            style="?android:attr/progressBarStyle"
            android:layout_width="30dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="@{newsTotalPageOuted ? View.GONE : View.VISIBLE}"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:fontFamily="monospace"
            android:text="@string/scroll_to_end_text"
            android:textColor="@color/scrollToEndText"
            android:textSize="20sp"
            android:textStyle="italic"
            android:visibility="@{newsTotalPageOuted ? View.VISIBLE : View.GONE}"/>
    </LinearLayout>
</layout>